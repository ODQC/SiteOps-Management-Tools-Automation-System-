export const environment = {
  production: true,
  // Served from the same App Service as the backend (app-oscar-sc500-lab), so the API
  // is same-origin — no host/port to hardcode.
  apiUrl: '/'
};
